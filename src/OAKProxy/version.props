<Project>
  <PropertyGroup>
    <Version>1.0.0</Version>
    <FileVersion Condition=" '$(BUILD_BUILDID)' == '' ">developer</FileVersion>
    <FileVersion Condition=" '$(BUILD_BUILDID)' != '' ">pipeline-$(BUILD_BUILDID)</FileVersion>
  </PropertyGroup>
</Project>